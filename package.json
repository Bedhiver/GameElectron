{
    "name": "gameelectron",
    "version": "0.1.0",
    "main": "main.js",
    "scripts": {
        "start": "electron .",
        "dev": "APP_DEV=true electron .",
        "build": "electron-packager ./ game --platform=linux --arch=x64 --prune=true --ignore=\"(\\.gitignore|\\.vscode)\" --asar"
    },
    "devDependencies": {
        "electron": "^11.0.3",
        "electron-packager": "^15.2.0",
        "electron-reload": "^1.5.0"
    },
    "dependencies": {
        "aws-sdk": "^2.804.0",
        "phaser": "^3.24.1"
    }
}
